@model List<Laser.Orchard.UserReactions.ViewModels.UserReactionsVM>


@foreach (var item in Model) { 
    @item.Quantity<text> </text>
    @item.TypeName <text> </text>
    @item.TypeId <text> </text><br />
}