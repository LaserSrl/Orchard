@{
     Style.Require("Reactions");
}

@{
    Laser.Orchard.UserReactions.Models.UserReactionsPart part = Model.UserReaction;
}

@{foreach (var item in part.Reactions) 
  { 
    
    var quantità = "(" + @item.Quantity + ")";
    var itemName = " " + item.UserReactionsTypesRecord.TypeName + " " + quantità;
        
      <span id="@item.UserReactionsTypesRecord.TypeName">@item.UserReactionsTypesRecord.TypeName</span>
    } 
} 


@*@foreach (var item in part.Reactions) { 
    @item.Quantity
    @item.UserReactionsTypesRecord.TypeName
}*@