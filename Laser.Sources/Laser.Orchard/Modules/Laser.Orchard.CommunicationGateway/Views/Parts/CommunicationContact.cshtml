@foreach (var field in Model.ContentPart.Fields) {
    if(field.FieldDefinition.Name == "TaxonomyField") {
        @Display.Fields_TaxonomyField(Field: field)
    }
}
<fieldset>
    <legend>@T("Logs")</legend>
    <span class="text large">@Model.Logs</span>
</fieldset>
