using Orchard.ContentManagement;

namespace Contrib.Profile.Models {
    //public class ProfilePart : ContentPart {
    //    //We only need to create the part so it's recognized in handlers and such
    //    //This allows doing item.As<ProfilePart>() rather than go through its parts
    //    //looking for the part called "ProfilePart"
    //}
}